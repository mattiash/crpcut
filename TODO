* Compatibility
  - OS/X would be nice

* Features:
  - Regexp match rules for stdout and stderr (probably won't happen
    before 1.0)
  - ULPS difference comparison for ieee754 floating point values.
